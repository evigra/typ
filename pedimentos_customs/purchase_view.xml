<openerp>
<data>

    <!-- HERENCIA AL METODO RECIBIR PRODUCTOS EN COMPRAS -->
    <!-- <record id="view_purchase_inherit_typ_inhrt" model="ir.ui.view"> -->
    <!--         <field name="name">stock.partial.picking.inherit</field> -->
    <!--         <field name="model">stock.partial.picking</field> -->
    <!--         <field name="type">form</field> -->
    <!--         <field name="inherit_id" ref="stock.stock_partial_picking_form"/> -->
    <!--         <field name="arch" type="xml"> -->
    <!--             <field name="move_ids" position="before"> -->
    <!--                 <group col="4"> -->
    <!--                     <field name="picking_id" invisible="1" /> -->
    <!--                     <field name="picking_type" invisible="1" /> -->
                        <!-- <field name="pedimento_id" on_change="on_change_pedimento(pedimento_id,move_ids,picking_id,picking_type)" attrs="{'invisible':[('picking_type','!=','in')]}"/> -->
                    <!-- </group> -->
                <!-- </field> -->
                <!-- <xpath expr="//field[@name='move_ids']/tree/field[@name='prodlot_id']" position="after">
                <field name="pedimento_id"/>
            </xpath>  -->
            <!-- </field> -->
        <!-- </record> -->

        <!-- <record id="view_split_in_lots_typ_inhrt" model="ir.ui.view">
            <field name="name">stock.move.split.lines.inherit</field>
            <field name="model">stock.move.split</field>
            <field name="type">form</field>
            <field name="inherit_id" ref="stock.view_split_in_lots"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='line_exist_ids']/tree/field[@name='prodlot_id']" position="after">
                    <field name="pedimento_id"/>
                </xpath>
            </field>
        </record> -->

    <!-- ==== HERENCIA A COMPRAS ====== -->

        <!-- <record id="view_purchase_inherit_typ_inhrt" model="ir.ui.view">
            <field name="name">purchase.order.typ.button</field>
            <field name="model">purchase.order</field>
            <field name="type">form</field>
            <field name="inherit_id" ref="purchase.purchase_order_form"/>
            <field name="arch" type="xml">

            </field>
        </record> -->


</data>
</openerp>
