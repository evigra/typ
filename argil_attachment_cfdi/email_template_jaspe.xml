<?xml version="1.0" encoding="utf-8"?>
<openerp>
    <data noupdate="1">
        <!--Email template -->
        <record id="email_template_edi_invoice_jasper" model="email.template">
            <field name="name">CFDI-Send-by-Email</field>
            <field name="email_from">${object.user_id.email or object.company_id.email or 'noreply@localhost'}</field>
            <field name="subject">${object.company_id.name} Invoice (Ref ${object.number or 'n/a'})</field>
            <field name="email_recipients">${object.partner_id.id}</field>
            <field name="model">account.invoice</field>
            <field name="res_id">${object.id}</field>
            <field name="model_id" ref="account.model_account_invoice"/>
            <field name="auto_delete" eval="True"/>
            <!-- <field name="report_template" ref="account_invoice_facturae_dm3_jasper"/> -->
            <field name="report_template" ref=""/>
            <field name="report_name">${(object.company_emitter_id.address_invoice_parent_company_id.vat or '').replace('MX','')}_${object.journal_id.sequence_id.approval_id.serie}_${object.number}.pdf</field>
            <field name="lang">${object.partner_id.lang}</field>
            <field name="body_html"><![CDATA[
<div style="font-family: 'Lucica Grande', Ubuntu, Arial, Verdana, sans-serif; font-size: 12px; color: rgb(34, 34, 34); background-color: #FFF; ">

    <p>Notificacion para ${object.partner_id.name},</p>

    <p>Tenemos una Nueva Factura para Usted: </p>
    
    <p style="border-left: 1px solid #8e0000; margin-left: 30px;">
       &nbsp;&nbsp;<strong>DETALLES</strong><br />
       &nbsp;&nbsp;Numero de CFDI: <strong>${object.number}</strong><br />
       &nbsp;&nbsp;Monto Total: <strong>${object.amount_total} ${object.currency_id.name}</strong><br />
       &nbsp;&nbsp;Fecha Factura: ${object.date_invoice}<br />
       % if object.origin:
       &nbsp;&nbsp;Order reference: ${object.origin}<br />
       % endif
       % if object.user_id:
       &nbsp;&nbsp;Contacto: <a href="mailto:${object.user_id.email or ''}?subject=Invoice%20${object.number}">${object.user_id.name}</a>
       % endif
    </p>  
    
    % if object.paypal_url:
    <br/>
    <p>It is also possible to directly pay with Paypal:</p>
        <a style="margin-left: 120px;" href="${object.paypal_url}">
            <img class="oe_edi_paypal_button" src="https://www.paypal.com/en_US/i/btn/btn_paynowCC_LG.gif"/>
        </a>
    % endif
    
    <br/>
    <p>Para cualquier Duda o Aclaracion contacte al Departamento de Facturación</p>
    <p>Gracias por Elegir ${object.company_id.name or 'us'}!</p>
    <br/>
    <br/>
    <div style="width: 375px; margin: 0px; padding: 0px; background-color: #8E0000; border-top-left-radius: 5px 5px; border-top-right-radius: 5px 5px; background-repeat: repeat no-repeat;">
        <h3 style="margin: 0px; padding: 2px 14px; font-size: 12px; color: #DDD;">
            <strong style="text-transform:uppercase;">${object.company_id.name}</strong></h3>
    </div>
    <div style="width: 347px; margin: 0px; padding: 5px 14px; line-height: 16px; background-color: #F2F2F2;">
        <span style="color: #222; margin-bottom: 5px; display: block; ">
        % if object.company_id.street:
            ${object.company_id.street}<br/>
        % endif
        % if object.company_id.street2:
            ${object.company_id.street2}<br/>
        % endif
        % if object.company_id.city or object.company_id.zip:
            ${object.company_id.zip} ${object.company_id.city}<br/>
        % endif
        % if object.company_id.country_id:
            ${object.company_id.state_id and ('%s, ' % object.company_id.state_id.name) or ''} ${object.company_id.country_id.name or ''}<br/>
        % endif
        </span>
        % if object.company_id.phone:
            <div style="margin-top: 0px; margin-right: 0px; margin-bottom: 0px; margin-left: 0px; padding-top: 0px; padding-right: 0px; padding-bottom: 0px; padding-left: 0px; ">
                Phone:&nbsp; ${object.company_id.phone}
            </div>
        % endif
        % if object.company_id.website:
            <div>
                Web :&nbsp;<a href="${object.company_id.website}">${object.company_id.website}</a>
            </div>
        %endif
        <p></p>
    </div>
</div>
            ]]></field>
        </record>


<!-- # # # # # # # # # # # # # # ENVIAR POR CORREO PARA LOS EMPLEADOS CON CFDI # # # # # # # # # # # # # #  -->
    <record id="email_template_edi_payslip_cfdi" model="email.template">
            <field name="name">CFDI-Employee-Send-by-Email ${object.number or 'n/a'}</field>
            <field name="email_from">${object.company_id.email or 'noreply@localhost'}</field>
            <field name="subject">${object.company_id.name} Nomina CFDI (Ref ${object.number or 'n/a'} - ${object.partner_id.name})</field>
            <field name="email_recipients">${object.partner_id.id}</field>
            <field name="model_id" ref="hr_payroll.model_hr_payslip"/>
            <field name="auto_delete" eval="True"/>
            <field name="model">hr.payslip</field>
            <field name="res_id">${object.id}</field>
            <field name="report_template" ref=""/> <!-- Aqui va el reporte -->
            <field name="report_name">${(object.name)}_${object.number}.pdf</field>
            <field name="lang">${object.partner_id.lang}</field>
            <field name="body_html"><![CDATA[
<div style="font-family: 'Lucica Grande', Ubuntu, Arial, Verdana, sans-serif; font-size: 12px; color: rgb(34, 34, 34); background-color: #FFF; ">

    <p>Notificacion para! ${object.partner_id.name},</p>

    <p>Tenemos un Nuevo Recibo de Nomina CFDI: </p>
    
    <p style="border-left: 1px solid #8e0000; margin-left: 30px;">
       &nbsp;&nbsp;<strong>DETALLES</strong><br />
       &nbsp;&nbsp;Contrato: <strong>${object.contract_id.name}</strong><br />
       &nbsp;&nbsp;Numero CFDI: <strong>${object.number}</strong><br />
       &nbsp;&nbsp;Percepciones: <strong>${object.perception_total} ${object.currency_id.name}</strong><br />
       &nbsp;&nbsp;Deducciones: <strong>${object.deduction_total} ${object.currency_id.name}</strong><br />
       &nbsp;&nbsp;Total: <strong>${object.total} ${object.currency_id.name}</strong><br />
       &nbsp;&nbsp;Fecha y Hora: ${object.payslip_datetime}<br />
       &nbsp;&nbsp;Periodo: ${object.date_from} / ${object.date_to}<br />
       % if object.company_id:
       &nbsp;&nbsp;Your contact: <a href="mailto:${object.company_id.email or 'noreply@localhost'}">${object.company_id.name}</a>
       % endif
    </p>  
    
    % if object.paypal_url:
    <br/>
    <p>It is also possible to directly pay with Paypal:</p>
        <a style="margin-left: 120px;" href="${object.paypal_url}">
            <img class="oe_edi_paypal_button" src="https://www.paypal.com/en_US/i/btn/btn_paynowCC_LG.gif"/>
        </a>
    % endif
    
    <br/>
    <p>Para Cualquier Duda o Aclaracion Contacte al Departamento de RH.</p>
    <p>Gracias por Elegir ${object.company_id.name or 'us'}!</p>
    <br/>
    <br/>
    <div style="width: 375px; margin: 0px; padding: 0px; background-color: #8E0000; border-top-left-radius: 5px 5px; border-top-right-radius: 5px 5px; background-repeat: repeat no-repeat;">
        <h3 style="margin: 0px; padding: 2px 14px; font-size: 12px; color: #DDD;">
            <strong style="text-transform:uppercase;">${object.company_id.name}</strong></h3>
    </div>
    <div style="width: 347px; margin: 0px; padding: 5px 14px; line-height: 16px; background-color: #F2F2F2;">
        <span style="color: #222; margin-bottom: 5px; display: block; ">
        % if object.company_id.street:
            ${object.company_id.street}<br/>
        % endif
        % if object.company_id.street2:
            ${object.company_id.street2}<br/>
        % endif
        % if object.company_id.city or object.company_id.zip:
            ${object.company_id.zip} ${object.company_id.city}<br/>
        % endif
        % if object.company_id.country_id:
            ${object.company_id.state_id and ('%s, ' % object.company_id.state_id.name) or ''} ${object.company_id.country_id.name or ''}<br/>
        % endif
        </span>
        % if object.company_id.phone:
            <div style="margin-top: 0px; margin-right: 0px; margin-bottom: 0px; margin-left: 0px; padding-top: 0px; padding-right: 0px; padding-bottom: 0px; padding-left: 0px; ">
                Phone:&nbsp; ${object.company_id.phone}
            </div>
        % endif
        % if object.company_id.website:
            <div>
                Web :&nbsp;<a href="${object.company_id.website}">${object.company_id.website}</a>
            </div>
        %endif
        <p></p>
    </div>
</div>
            ]]></field>
        </record>

    </data>
</openerp>
